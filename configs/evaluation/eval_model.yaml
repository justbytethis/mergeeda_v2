# Configuration for base model evaluation

defaults:
  - model: qwen_vl_model

# Hydra output directory configuration
hydra:
  run:
    dir: data/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

# Input: directory containing evaluation Question JSON files
questions_dir: "data/datasets/amba_document/question_set/IHI0022L_amba_axi_protocol_spec"

# Input: directory containing extracted material files (images, tables)
materials_dir: "data/datasets/amba_document/processed/IHI0022L_amba_axi_protocol_spec/materials"

# Input: directory containing source chunk text files (for LLM judge context)
chunks_dir: "data/datasets/amba_document/processed/IHI0022L_amba_axi_protocol_spec/chunks"

# Output directory for preds.json and scores.json
output_dir: "data/evaluation/base_model_rag/IHI0022L_amba_axi_protocol_spec"

# If true, prepend "Specification: <source chunk content>" to each question
# using the markdown file referenced by item["source_chunk"] in chunks_dir.
include_specification: false

# LLM judge model configuration (OpenAI)
judge:
  name: "gpt-5.1"
  # API key is read from OPENAI_API_KEY env variable if not set here
  api_key: null
  # Number of parallel worker threads for concurrent API calls
  max_workers: 20
